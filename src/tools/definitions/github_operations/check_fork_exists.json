{
	"name": "check_fork_exists",
	"description": "Check if fork exists using GitHub API.",
	"parameters": {
		"type": "object",
		"properties": {
			"owner": {
				"type": "string",
				"description": "Owner of the repository"
			},
			"repo_name": {
				"type": "string",
				"description": "Name of the repository"
			}
		},
		"required": ["owner", "repo_name"]
	}
}
