{
	"name": "get_pr_template",
	"description": "Get the PR template content from .github/ directory or copy it from docs/agent/pr_template.md.",
	"input_schema": {
		"type": "object",
		"properties": {
			"repo_path": {
				"type": "string",
				"description": "Path to the git repository"
			}
		}
	}
}
