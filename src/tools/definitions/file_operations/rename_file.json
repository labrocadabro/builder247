{
	"name": "rename_file",
	"description": "Rename a file from source to destination, creating parent directories if needed.",
	"parameters": {
		"type": "object",
		"properties": {
			"source": {
				"type": "string",
				"description": "Current file path"
			},
			"destination": {
				"type": "string",
				"description": "New file path"
			}
		},
		"required": ["source", "destination"]
	}
}
